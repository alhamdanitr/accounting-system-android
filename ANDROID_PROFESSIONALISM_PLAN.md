# خطة احترافية لتطوير واجهات تطبيق الأندرويد
## مكمّلة لوثيقة ANDROID_ERP_UI_UX_COMPLETE_REDESIGN.md الموجودة في مستودعك

> هذه الخطة **لا تكرر** وثيقتك الحالية (وهي ممتازة وشاملة من ناحية الـ UX/Information Architecture)، بل تضيف الطبقة التقنية الاحترافية التي تحتاجها أي واجهة Enterprise حقيقية ولم تُذكر بعمق في الوثيقة الأصلية: الأداء، الاختبار البصري، إمكانية الوصول، التوطين، وإدارة نظام التصميم كـ "منتج" وليس مجرد ألوان.

---

## المرحلة 0 — تنظيف المستودع (قبل أي شيء)
**المدة: نصف يوم**

1. حذف `full_android_log.txt` و`latest_android_log.txt` من التتبع، وإضافة `*.txt` لملفات اللوق ضمن `.gitignore` (مع استثناء أي ملف نصي مطلوب فعلاً).
2. نقل `MASTER_WINDOWS_ERP_UI_INSTRUCTIONS.md` إلى مستودع الويندوز الصحيح.
3. مراجعة `.gitignore` للتأكد من استثناء `local.properties`, `*.keystore`, `build/`, `.gradle/`.
4. تفعيل حماية الفرع الرئيسي (نفس مبدأ مستودع الباك اند).

---

## المرحلة 1 — نظام التصميم كـ "مصدر حقيقة" (Design Tokens)
**المدة: 3-5 أيام | الأساس الذي يبني عليه كل شيء لاحقًا**

وثيقتك الحالية تذكر "Design System" كبند (رقم 31) لكن بدون آلية تقنية. هنا التفصيل:

1. بناء ملف tokens مركزي في Compose (ليس قيم متفرقة داخل كل Composable):
   ```kotlin
   object AppColors { val primary = ... ; val surface = ... ; val financialPositive = ... ; val financialNegative = ... }
   object AppSpacing { val xs = 4.dp; val sm = 8.dp; val md = 16.dp; val lg = 24.dp; val xl = 32.dp }
   object AppTypography { val displayLarge = ...; val kpiValue = ...; val bodyArabic = ... }
   ```
2. بناء `MaterialTheme` مخصص كامل (Light/Dark) مبني على هذه الـ tokens، بحيث أي تغيير مستقبلي (تغيير لون العلامة التجارية مثلاً) يتم في مكان واحد فقط.
3. تعريف ألوان دلالية خاصة بالمحاسبة تحديدًا (هذه نقطة غائبة عن وثيقتك):
   - لون موحّد لكل "مبلغ مستحق" (Due)
   - لون موحّد لكل "رصيد سالب/دين"
   - لون موحّد لكل "حالة متزامن/غير متزامن"
   - يجب أن تكون نفس الألوان بالضبط تُستخدم في تطبيق الويندوز لاحقًا (اتساق بصري بين المنصتين).
4. توثيق كل هذا في `ANDROID_DESIGN_TOKENS.md` (يُضاف لقائمة ملفاتك التوثيقية الموجودة أصلاً).

### معيار الإغلاق
- [ ] لا يوجد أي `Color(0xFF...)` أو `.sp` مكتوب مباشرة داخل أي Composable شاشة — كل شيء يمر عبر الـ theme.

---

## المرحلة 2 — مكتبة المكونات كـ Compose Preview Gallery
**المدة: 1 أسبوع**

وثيقتك (بند 35) تسرد أسماء المكونات المطلوبة (KpiCard, StatusChip...) — هذه المرحلة تجعلها **قابلة للمراجعة البصرية فعليًا**:

1. لكل مكوّن مشترك، بناء `@Preview` متعدد الحالات (فاتح/غامق، RTL، بيانات طويلة، بيانات فارغة) في نفس الملف.
2. بناء شاشة داخلية مخفية (`ComponentGalleryScreen`) تعرض كل المكونات في مكان واحد — تُستخدم فقط في نسخة Debug، تسهّل مراجعة الاتساق البصري لأي مكوّن جديد قبل استخدامه في شاشة حقيقية.
3. قاعدة صارمة: أي Composable يُستخدم في أكثر من شاشة واحدة يجب أن يكون مكوّنًا مشتركًا موثقًا في المعرض، لا نسخة مكررة يدويًا.

### معيار الإغلاق
- [ ] كل مكوّن في قائمة البند 35 من وثيقتك له Preview فعلي بحالاته الأربع (فاتح، غامق، فارغ، ممتلئ).

---

## المرحلة 3 — الأداء (Performance) — غائب تمامًا عن الوثيقة الأصلية
**المدة: مستمرة، مراجعة أسبوعية**

نظام محاسبي فيه جداول طويلة (فواتير، منتجات، حركات مخزون) — الأداء هنا أمر حرج وليس رفاهية:

1. استخدام `LazyColumn`/`LazyVerticalGrid` مع `key` صريح لكل عنصر (تفادي إعادة الرسم غير الضرورية).
2. تفادي القراءات الثقيلة داخل composition نفسه — كل بيانات القوائم تأتي من `StateFlow` جاهزة، لا حسابات ثقيلة داخل الـ Composable.
3. استخدام `derivedStateOf` عند الحاجة لتفادي إعادة الحساب المتكررة (مثال: مجموع الفاتورة أثناء الكتابة).
4. تحميل الصور (شعارات المنتجات إن وُجدت) عبر Coil مع caching صحيح، وليس Bitmap يدوي.
5. قياس الأداء الفعلي عبر Layout Inspector / Macrobenchmark على الشاشات الأثقل (POS، قائمة المنتجات) على جهاز متوسط المواصفات وليس فقط جهاز تطوير قوي.

### معيار الإغلاق
- [ ] شاشة القوائم الطويلة (منتجات، فواتير) تعمل بسلاسة 60fps على جهاز متوسط مع 500+ عنصر.

---

## المرحلة 4 — الاختبار البصري (Visual Regression Testing) — غائب عن الوثيقة
**المدة: 3-5 أيام إعداد + مستمر بعدها**

وثيقتك تطلب "Visual QA" يدوي (بند 46) لكن هذا لا يكفي لمشروع طويل الأمد — أي تعديل بسيط قد يكسر شاشة أخرى بدون أن يلاحظ أحد:

1. دمج **Paparazzi** أو **Compose Preview Screenshot Testing** الرسمي من جوجل ضمن المشروع.
2. أخذ لقطات مرجعية (baseline) لكل شاشة رئيسية بحالاتها (فاتح/غامق/RTL/فارغ).
3. ربط هذا الاختبار بـ CI (`.github/workflows`) بحيث أي PR يغيّر شكل شاشة بدون قصد يُكتشف تلقائيًا قبل الدمج.
4. هذا يحوّل "بند 46 Visual QA Checklist" من عملية يدوية متعبة إلى فحص آلي دائم.

### معيار الإغلاق
- [ ] كل شاشة رئيسية لها لقطة مرجعية، وأي PR يكسرها يُرفض تلقائيًا في CI.

---

## المرحلة 5 — إمكانية الوصول (Accessibility) — غائبة تمامًا عن الوثيقة
**المدة: 3-4 أيام**

مهمة لأي تطبيق تجاري احترافي حقيقي، وتُهمل عادة:

1. التأكد أن كل عنصر تفاعلي له `contentDescription` مناسب بالعربية.
2. اختبار التطبيق فعليًا مع TalkBack (قارئ الشاشة) على الأقل على الشاشات الرئيسية (POS، الفواتير، الداشبورد).
3. دعم تكبير الخط من إعدادات النظام (Font Scale) دون كسر التخطيط — اختبار عند 130%-150%.
4. التأكد من تباين الألوان (Contrast Ratio) كافٍ خصوصًا للأرقام المالية المهمة (لا رمادي فاتح على أبيض لمبلغ مستحق).
5. أحجام لمس لا تقل عن 48dp لكل عنصر تفاعلي (وثيقتك ذكرت هذا في بند 39 لكن دون رقم محدد — هذا هو المعيار الرسمي من Material).

### معيار الإغلاق
- [ ] TalkBack يقدر يقرأ ويُكمل عملية بيع كاملة من البداية للنهاية بدون ارتباك.

---

## المرحلة 6 — التوطين والبنية النصية (Localization Architecture)
**المدة: 2-3 أيام**

وثيقتك تمنع خلط العربية والإنجليزية بصريًا (بند 8) — هذه المرحلة تضمن ذلك تقنيًا وليس فقط تصميميًا:

1. كل نص ظاهر للمستخدم يجب أن يكون في `strings.xml` عبر `stringResource()` — ممنوع أي نص مكتوب مباشرة (hardcoded) داخل Composable.
2. تجهيز البنية لدعم لغة إنجليزية مستقبلية بسهولة (حتى لو العربية فقط الآن) — `values-en/strings.xml` جاهز كهيكل من الآن.
3. معالجة خاصة للأرقام: استخدام `NumberFormat` مرتبط بـ Locale لعرض الأرقام والعملة بشكل صحيح (فرق بين الأرقام الهندية والعربية الغربية حسب إعداد الجهاز — نقطة كثيرًا ما تُنسى).
4. معالجة التواريخ عبر `DateTimeFormatter` مرتبط بالـ Locale أيضًا.

### معيار الإغلاق
- [ ] صفر نص عربي أو إنجليزي مكتوب مباشرة داخل كود Compose.

---

## المرحلة 7 — التنفيذ الفعلي وفق تسلسل وثيقتك (Phase 1-12 الأصلية)
**المدة: الأطول — عدة أسابيع**

هنا تُنفَّذ المراحل الـ12 التي حددتها وثيقتك (Audit → Design System → App Shell → Dashboard → Sales → Inventory → ...) لكن **بعد** تطبيق المراحل 1-6 أعلاه كطبقة تحتية، بحيث كل شاشة تُبنى من أول يوم على design tokens صحيحة ومكونات موثقة وقابلة للاختبار البصري، بدل بنائها ثم اكتشاف الحاجة لإعادة الهيكلة لاحقًا.

**نصيحة تنفيذية مهمة:** رتّب شاشاتك حسب "الأكثر استخدامًا يوميًا" وليس فقط الترتيب المنطقي — الداشبورد ونقطة البيع (POS) يجب أن يُنفَّذا بأعلى جودة أولاً لأنهما الواجهة التي يراها المستخدم كل دقيقة تقريبًا، وهما ما يشكّل الانطباع الأول عن احترافية التطبيق.

---

## المرحلة 8 — دعم الشاشات المختلفة (Responsive/Adaptive)
**المدة: 2-3 أيام**

غير مذكورة في وثيقتك — مهمة إذا كان التطبيق سيُستخدم على أجهزة تابلت في المحل (شاشة كاشير أكبر مثلاً):

1. استخدام `WindowSizeClass` من Compose Adaptive لتكييف التخطيط بين الهاتف والتابلت.
2. شاشة POS تحديدًا تستفيد كثيرًا من تخطيط ثنائي العمود على الشاشات الأكبر (قائمة المنتجات + السلة جنبًا لجنب) بدل التمرير العمودي فقط.

### معيار الإغلاق
- [ ] القياس على تابلت 10 بوصة لا يظهر تخطيطًا مكسورًا أو هاتفًا "مكبّرًا" فقط.

---

## جدول زمني تقديري إجمالي

| المرحلة | المدة |
|---|---|
| 0 — تنظيف المستودع | نصف يوم |
| 1 — Design Tokens | 3-5 أيام |
| 2 — مكتبة المكونات | 1 أسبوع |
| 3 — الأداء | مستمر |
| 4 — الاختبار البصري | 3-5 أيام إعداد |
| 5 — إمكانية الوصول | 3-4 أيام |
| 6 — التوطين | 2-3 أيام |
| 7 — تنفيذ الشاشات (12 مرحلة من وثيقتك) | 4-8 أسابيع |
| 8 — دعم التابلت | 2-3 أيام |

**الأساس التقني (0-6) يحتاج حوالي 2-3 أسابيع قبل البدء الفعلي بإعادة بناء الشاشات** — استثمار يستحق لأنه يمنع إعادة العمل لاحقًا.

---

## الخلاصة

وثيقتك `ANDROID_ERP_UI_UX_COMPLETE_REDESIGN.md` ممتازة في تعريف **ماذا** يجب أن تبدو عليه كل شاشة (الشكل، البنية، المحتوى). هذه الخطة تضيف **كيف تُبنى بشكل يدوم** (الأداء، الاختبار الآلي، إمكانية الوصول، إدارة نظام التصميم كمنتج). الاثنان معًا يشكّلان خطة كاملة فعلاً لتطبيق ERP احترافي حقيقي وليس Prototype.
